<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">
    <application
            android:allowBackup="true"
            android:name=".SmartPlantApplication"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.SmartPlantAndroid.Light"
            android:networkSecurityConfig="@xml/network_security_config"
            tools:targetApi="31">
        <activity
                android:name="com.smartplant.feature_init.ui.InitActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
                android:name="com.smartplant.feature_start.ui.StartActivity"
                android:exported="true">
        </activity>
        <activity
                android:name="com.smartplant.feature_auth.ui.AuthActivity"
                android:exported="true">
        </activity>
        <activity
                android:name="com.smartplant.feature_main.ui.MainActivity"
                android:exported="true">
        </activity>
    </application>
</manifest>
